# 2026-03-01 Notes

## Decisions Made

- John's agent is configured to run `ollama/qwen3-coder:30b` model for all tasks.
- John will follow the workflow defined in `HEARTBEAT.md` for task execution.
- John will use `Aider` with the `ollama_chat/qwen3-coder:30b` model for code editing tasks.
- John will use the `aider-job.sh` script to execute Aider jobs.
- John will not use Anthropic models, always using `ollama_chat/qwen3-coder:30b`.
- John will verify that Aider actually committed before marking a task as Done.
- John will not disturb the user between 23:00-08:00.
- John will take tasks from the QUEUE.md in order, never cherry-picking.
- John will move tasks from "Ready" to "In Progress" before starting.
- John will move tasks to "Done" after successful completion and reporting.
- John will report task status to MC via a curl command.
- John will not use any tools that require elevated permissions unless explicitly needed.

## Workflows Agreed

- John will execute tasks from the QUEUE.md in order.
- John will execute Aider jobs with specific parameters:
  - `AIDER_NO_AUTO_UPDATE=1`
  - `OLLAMA_API_BASE=http://localhost:11434`
  - `aider --no-pretty --no-fancy-input --model ollama_chat/qwen3-coder:30b --no-check-update --yes --auto-commits --file <FIL> --message "<UPPGIFT>"`
- John will verify commit after Aider execution.
- John will push changes to the main branch after Aider execution.
- John will report task completion to MC via a curl POST request.
- John will move tasks through the workflow: Ready → In Progress → Done.

## Infra Details

- John's workspace is `/Users/diffen/.openclaw/workspace-john`.
- John's repos are:
  - MFE-host: `/tmp/mfe-host`
  - Mission Control: `/tmp/mission-control`
  - AF-portal: `/tmp/af-portal`
  - Trading-agent: `/tmp/trading-agent`
  - Reko-saas: `/tmp/reko-saas`
- John's scripts are located in `/Users/diffen/.openclaw/workspace/scripts/`.
- John's Aider job script is `/Users/diffen/.openclaw/workspace/scripts/aider-job.sh`.
- John's model is served via Ollama at `http://localhost:11434`.
- John's MC reporting endpoint is `http://192.168.99.4:3200/api/office`.

## Port Mappings

- Ollama API is exposed on port `11434`.
- MC reporting endpoint is exposed on port `3200`.

## Specs Discussed

- John will use `Aider` with `ollama_chat/qwen3-coder:30b` model for all code editing tasks.
- John will not use any other models or tools unless explicitly instructed.
- John will execute tasks in the order they appear in the QUEUE.md.
- John will not interrupt the user during quiet hours (23:00-08:00).
- John will report task status to MC after each successful completion.

## Additional Notes

- The previous session included a minor delay due to gateway issues, but the current setup is stable.
- John is now properly configured and ready to execute tasks according to the established workflow.